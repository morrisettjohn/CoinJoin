/**
 * @packageDocumentation
 * @module Utils-HDKey
 */
import { Buffer } from 'buffer/';
import { HDKeyJSON } from 'src/common';
import { default as hdkey } from 'hdkey';
import HDNode from 'hdkey';
export { HDNode };
/**
 * BIP32 hierarchical deterministic keys.
 *
 */
export default class HDKey {
    private static instance;
    private constructor();
    /**
     * Retrieves the HDKey singleton.
     */
    static getInstance(): HDKey;
    /**
     * Creates an hdkey object from a master seed buffer
     *
     * @param seedBuffer
     *
     * @returns
     */
    fromMasterSeed(seedBuffer: Buffer): hdkey;
    /**
     * Creates an hdkey object from an object created via hdkey.toJSON().
     *
     * @param obj
     *
     * @returns
     */
    fromJSON(obj: HDKeyJSON): hdkey;
}
//# sourceMappingURL=hdkey.d.ts.map